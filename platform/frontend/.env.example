# 平台后端当前默认前缀是 /api/v1。
# 开发模式下 Vite 会把它代理到 `VITE_DEV_PROXY_TARGET`，默认是 http://127.0.0.1:8000。
# 如果你的部署环境做了额外代理改写，再按实际情况覆盖。
VITE_API_BASE_URL=/api/v1

# 第二阶段默认优先尝试真实接口，只有你明确想固定看 mock 时才设为 true。
VITE_USE_MOCK=false

# 可选：覆盖本地开发代理的后端目标地址。
# VITE_DEV_PROXY_TARGET=http://127.0.0.1:8000
